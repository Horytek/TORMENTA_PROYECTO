/**
 * OBSOLETO tras cutover a db_ecommerce (theme_json ya está en schema create).
 * Conservado solo como referencia; no ejecutar contra db_tormenta.
 */
console.error(
  "migrate_ecommerce_theme_json.js es obsoleto: theme_json vive en db_ecommerce.tienda (ver create_ecommerce_database.js)."
);
process.exit(1);
